How the web works
-client visiting browser
-domain checked in browser
-domain lookup
-browser sends request
-response to client
-http/s protocol: valid request
-(s) data encryption -ssl encryption
-core modules: fs, parth, os, http, https
-http : send request
-https: ssl server

Node  js Lifecycle:
Event loop- keeps on running as there are event listener registered.
-Node app use event loop
-single threaded is js.
-to make it always available uses event loop.
-process.exit to unregister

Request object:
-node js create this object when we access that url
-headers: meta-data

